{
  "name": "ai-executive-os",
  "private": true,
  "description": "One command: npm run dev | npm run prod — starts backend + frontend with matching env files.",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "prod": "concurrently -k -n backend,frontend -c blue,green \"npm run prod --prefix backend\" \"npm run prod --prefix frontend\"",
    "setup": "npm run setup --prefix backend && npm install --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "typecheck:backend": "npm run typecheck --prefix backend",
    "typecheck:frontend": "npm run typecheck --prefix frontend",
    "test:ci": "npm run test:ci --prefix backend && npm test --prefix frontend -- --ci",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
